{
    "scripts": {
        "build": "cd web && npm install && npm run build && cd ../backend && npm install && npm run vercel-build",
        "dev": "cd web && npm run dev",
        "start": "cd backend && npm run dev"
    }
}