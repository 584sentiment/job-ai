# API 基础 URL
# 请根据实际部署环境修改

# 开发环境（使用 Vite 代理到本地后端）
VITE_API_BASE_URL=/api

# 生产环境（Vercel 部署，使用同源相对路径）
# VITE_API_BASE_URL=/api

# 说明：
# 1. 本地开发：前端运行在 localhost:3000，后端运行在 localhost:8080
#    Vite 代理会将 /api 请求转发到 http://localhost:8080
#    配置见 vite.config.js 的 server.proxy
#
# 2. Vercel 部署：前端和后端在同一域名下
#    使用相对路径 /api，通过根目录 vercel.json 的 rewrites 规则
#    自动转发到 backend/api/index.ts
#
# 3. 这样配置的优势：
#    - 避免跨域问题（生产环境同源）
#    - 避免混合内容错误（HTTPS 页面请求 HTTPS API）
#    - 本地开发和生产环境使用相同的配置，自动适配
#
# 4. 路由规则（根目录 vercel.json）：
#    - /api/:path* → backend/api/index.ts（Express 后端）
#    - /:path* → web/dist/index.html（Vue 前端）
