# Vercel 部署指南

本项目已配置为可以部署到 Vercel，并从指定的后端 API 获取数据。

## 📋 前置要求

1. **后端 API 必须可以从公网访问**
   - 如果使用 natapp 等内网穿透服务，需要确保服务持续运行
   - 推荐使用云服务器部署后端，而不是内网穿透

2. **后端 API 地址**
   - 当前配置: `http://ybb9647b.natappfree.cc/job-track-assistant`
   - 如果需要修改，请更新以下配置

## 🚀 部署步骤

### 方法一：通过 Vercel CLI 部署

1. 安装 Vercel CLI
```bash
npm install -g vercel
```

2. 在项目根目录运行
```bash
cd web
vercel
```

3. 按照提示完成部署
   - 选择正确的项目设置
   - 确认构建命令为 `npm run build`
   - 输出目录为 `dist`

### 方法二：通过 Vercel 网站部署

1. 访问 [Vercel](https://vercel.com)
2. 登录并点击 "New Project"
3. 导入你的 Git 仓库
4. 配置项目：
   - **Framework Preset**: Vite
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
5. 添加环境变量（重要！）：
   - 在 "Environment Variables" 部分添加：
   - **Name**: `VITE_API_BASE_URL`
   - **Value**: `http://ybb9647b.natappfree.cc/job-track-assistant`
6. 点击 "Deploy" 开始部署

## ⚙️ 配置说明

### 环境变量

项目使用以下环境变量：

- **`VITE_API_BASE_URL`**: 后端 API 基础 URL
  - 开发环境: `/api` (使用 Vite 代理)
  - 生产环境: 完整的 API 地址

### 配置文件

1. **`.env.production`**: 生产环境配置（本地）
2. **`vercel.json`**: Vercel 部署配置
3. **`vite.config.js`**: Vite 构建配置

### API 代理配置

在开发环境中，Vite 会将 `/api` 请求代理到后端：

```javascript
// vite.config.js
proxy: {
  '/api': {
    target: 'http://ybb9647b.natappfree.cc',
    changeOrigin: true,
    rewrite: (path) => path.replace(/^\/api/, '/job-track-assistant')
  }
}
```

## 🔧 修改后端 API 地址

如果需要修改后端 API 地址，有以下几种方式：

### 方式 1：修改 Vercel 环境变量（推荐）

1. 登录 Vercel
2. 进入项目设置
3. 找到 "Environment Variables"
4. 修改 `VITE_API_BASE_URL` 的值
5. 重新部署项目

### 方式 2：修改代码

1. 修改 `.env.production` 文件：
```bash
VITE_API_BASE_URL=http://your-api-domain.com/api
```

2. 修改 `vercel.json` 文件中的 `env.value`

3. 重新部署

## ⚠️ 注意事项

### 1. 跨域问题

如果后端 API 没有配置 CORS，可能会遇到跨域问题。请确保后端已配置允许前端域名的跨域请求。

### 2. HTTPS

如果前端使用 HTTPS（Vercel 自动提供），后端 API 也应该使用 HTTPS。如果后端只支持 HTTP，浏览器可能会阻止混合内容请求。

### 3. 内网穿透服务稳定性

使用 natapp 等内网穿透服务时：
- 服务不稳定可能导致前端无法访问后端
- 隧道地址可能会变化
- 不推荐在生产环境使用

推荐使用云服务器部署后端，并配置稳定的域名和 SSL 证书。

## 📦 本地构建测试

在部署前，可以先在本地测试生产环境构建：

```bash
# 构建生产版本
npm run build

# 预览构建结果
npm run preview
```

## 🔄 持续部署

配置好 Git 仓库后，每次推送到主分支，Vercel 会自动重新部署项目。

## 📝 常见问题

### Q: 部署后无法连接到后端 API？

A: 检查以下几点：
1. 后端 API 是否可以从公网访问
2. Vercel 环境变量是否配置正确
3. 后端 API 是否配置了 CORS
4. 浏览器控制台是否有错误信息

### Q: 如何查看部署后的环境变量？

A: 在 Vercel 项目设置 -> Environment Variables 中查看。

### Q: 如何回滚到之前的部署版本？

A: 在 Vercel 项目部署列表中，选择之前的版本点击 "Promote to Production"。

## 📞 支持

如有问题，请查看：
- [Vercel 文档](https://vercel.com/docs)
- [Vite 文档](https://vitejs.dev/)
